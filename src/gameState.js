function initState() {
    const state = {
        player: {
            width: 82,
            height: 100,
            posX: 200,
            posY: 200
        },
        keys: {
            
        }
    };

    return state;
}